from django.db import models
from django.contrib.auth.models import User #django has few tables built out for us, this takes care of the user authentication  
# Create your models here.

# class Task(models.Model):
#     user = models.ForeignKey(User, on_delete=models.CASCADE, null=True, blank=True)
#     title = 
#     description =
#     complete = 
#     create =
